import 'package:flutter/material.dart';
 
class AppColors {
  static const Color primary = Color(0xFF0A73FF); // Primary color (blue)
  static const Color secondary = Color(0xFF4CAF50); // Secondary color (green)
  static const Color background = Color(0xFFF4F4F4); // Background color (light grey)
  static const Color textPrimary = Color(0xFF000000); // Primary text color (black)
  static const Color textSecondary = Color(0xFF757575); // Secondary text color (grey)
  static const Color accent = Color(0xFFFFC107); // Accent color (amber)
  static const Color error = Color(0xFFD32F2F); // Error color (red)
}